using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Poolable : MonoBehaviour
{
    //이 스크립트가 있으면 Pool 가능 오브젝트
    //이 자체로는 어떠한 기능도 없다.
    private void Start()
    {
        //GameManager.Pool.
    }
}
